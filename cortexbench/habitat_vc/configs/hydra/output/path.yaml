# @package hydra
run:
  dir: /home/dongwang/checkpoint/metaclip_exp/eai-vc/cortexbench/habitat_vc/results/${hydra.job.name}/${oc.env:USER}/${WANDB.name}
  subdir: ${hydra.job.num}_${hydra.job.override_dirname}
sweep:
  dir: /home/dongwang/checkpoint/metaclip_exp/eai-vc/cortexbench/habitat_vc/results/${hydra.job.name}/${oc.env:USER}/${WANDB.name}
  subdir: ${hydra.job.num}_${hydra.job.override_dirname}
